import { FiltrarCancionesPipe } from './filtrar-canciones.pipe';

describe('FiltrarCancionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarCancionesPipe();
    expect(pipe).toBeTruthy();
  });
});
